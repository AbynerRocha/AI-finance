// try {
//     const interaction = await ai.interactions.create({
//         model: 'gemini-2.5-flash',
//         input: "Em qual ano a primeira IA foi criada?"
//     });

//     return res.status(200).send({ message: interaction.output_text );
// } catch (error) {
//     console.error('Erro na interação:', error);
//     return res.status(500).send({ error: 'Falha na comunicação com a IA' });
// }
